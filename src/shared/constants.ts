export const APP_NAME = "uploady.app";

export const ID_PREFIXES = {
  user: "usr",
  magicLink: "mlk",
  session: "ses",
} as const;

export const COOKIE_NAMES = {
  refreshToken: "uploady_app_refresh_token",
} as const;
